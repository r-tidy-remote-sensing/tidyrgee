# tidyrgee 0.1.1

## Bug fixes

* Fixed missing package anchor in documentation cross-reference for `dplyr::group_by_drop_default()` in `group_by.Rd`
* Moved `reticulate` from Imports to Suggests, as it is only used in tests

# tidyrgee 0.1.0

* Added a `NEWS.md` file to track changes to the package.

First release
